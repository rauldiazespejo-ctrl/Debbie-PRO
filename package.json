{
  "name": "debbie-pro",
  "version": "1.0.0",
  "private": true,
  "description": "DEBBIE PRO — shell iOS (Capacitor) sobre la web en /public",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "sync:ios": "cap sync ios",
    "open:ios": "cap open ios",
    "ios": "cap sync ios && cap open ios",
    "build:ios": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
